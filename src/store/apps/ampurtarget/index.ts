//** Redux Imports */
import { Dispatch } from 'redux';
import { createSlice, createAsyncThunk } from '@reduxjs/toolkit';

export const appAmpurTargetSlice = createSlice({
    name: 'appAmpurtarget',
    initialState: 0,
    reducers: {},
})

export default appAmpurTargetSlice.reducer