import { Dispatch } from "redux";
import { createSlice, createAsyncThunk } from "@reduxjs/toolkit";

export const appMophCoordinatorSlice = createSlice({
    name: 'appMophCoordinator',
    initialState: 0,
    reducers: {},
})

export default appMophCoordinatorSlice.reducer