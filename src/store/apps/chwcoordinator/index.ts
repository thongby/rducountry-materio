import { Dispatch } from "redux";
import { createSlice, createAsyncThunk } from "@reduxjs/toolkit";

export const appChwCoordinatorSlice = createSlice({
    name: 'appChwCoordinator',
    initialState: 0,
    reducers: {},
})

export default appChwCoordinatorSlice.reducer